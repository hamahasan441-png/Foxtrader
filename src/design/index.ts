// ============================================================================
// FOX DESIGN SYSTEM — Barrel Export
//
// Usage in TypeScript:
//   import { palette, space, font, motion } from '../design';
//
// Usage in HTML:
//   <link rel="stylesheet" href="./design/fox-theme.css">
//   <link rel="stylesheet" href="./design/components.css">
//
// All visual properties flow from design tokens → CSS custom properties.
// Components use CSS vars (--fox-*), never raw values.
// ============================================================================

export { palette, space, font, elevation, radius, motion, zIndex, breakpoint } from './tokens';
